// Sekce 8 · Footer CTA (SPEC Sekce 8) — velký CTA „Těšíme se na spolupráci!" + footer bar.
// Placeholder kostra — implementace v Úkolu #7.

export function Section8Footer() {
  return (
    <section
      id="section-8"
      aria-label="Sekce 8 — Footer CTA"
      className="w-full min-h-screen flex items-center justify-center bg-neutral text-white"
    >
      <span className="text-label">Sekce 8 · Footer CTA</span>
    </section>
  );
}
