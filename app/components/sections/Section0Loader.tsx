// Sekce 0 · Loader (SPEC 0 → Sekce 0)
// Placeholder kostra — implementace v Úkolu #2.

export function Section0Loader() {
  return (
    <section
      id="section-0"
      aria-label="Sekce 0 — Loader"
      className="w-full min-h-screen flex items-center justify-center bg-secondary text-primary"
    >
      <span className="text-label">Sekce 0 · Loader</span>
    </section>
  );
}
