// Sekce 7 · Sky Guard (SPEC Sekce 7) — split 50/50 (video vlevo, text+CTA vpravo).
// Placeholder kostra — implementace v Úkolu #6.

export function Section7SkyGuard() {
  return (
    <section
      id="section-7"
      aria-label="Sekce 7 — Sky Guard"
      className="w-full min-h-screen flex items-center justify-center bg-offwhite text-secondary"
    >
      <span className="text-label">Sekce 7 · Sky Guard</span>
    </section>
  );
}
